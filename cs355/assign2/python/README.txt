Python version of a generalized queue.

The over-all goal is to make the queue able to handle arbitrary numbers of servers, queues, distribution functions, etc. as well as do both time and frequency division multiplexing.

